# Spring properties
spring:
  application:
     name: InfoService
# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
# HTTP Server
server:
  port: 8082   # HTTP (Tomcat) port